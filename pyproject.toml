[project]
name = "intropy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
intropy = "intropy:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ptpython>=3.0.32",
    "pytest>=9.1.1",
    "ruff>=0.16.4",
]
